def days_to_timstamp(nb_days: int) -> int:
    """
    The purpose of this function is to transform a number of days into a timestamp, that we will use in the ETL process.

    :param nb_days:
    :return:
    """
